<div class="cart-overlay" id="cart-overlay" onclick="closeCart()"></div>

<div class="cart-drawer" id="cart-drawer" aria-label="Carrinho">
  <div class="cart-drawer-header">
    <h2>Carrinho</h2>
    <button class="cart-close" onclick="closeCart()" aria-label="Fechar carrinho">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
  </div>

  <div class="cart-items" id="cart-items-list">
    <!-- Populated by JS -->
    <div class="cart-empty" id="cart-empty">
      <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" style="color:var(--muted-foreground)">
        <path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><line x1="3" y1="6" x2="21" y2="6"/>
        <path d="M16 10a4 4 0 0 1-8 0"/>
      </svg>
      <p>Seu carrinho está vazio</p>
      <a href="/collections/all" onclick="closeCart()" class="btn-primary btn-musas" style="border-radius:9999px;font-size:0.75rem;padding:0.75rem 1.5rem;">Ver Produtos</a>
    </div>
  </div>

  <div class="cart-footer" id="cart-footer" style="display:none;">
    <div id="cw-wholesale-progress-container"></div>

    <!-- Timer de reserva do carrinho -->
    <div id="cart-reserve-timer" style="display:none;align-items:center;gap:8px;margin-bottom:0.75rem;padding:8px 12px;border-radius:8px;border:1px solid #93C5FD;background:linear-gradient(135deg,#EFF6FF,#DBEAFE);font-size:0.75rem;">
      <span id="cart-reserve-icon">⏱️</span>
      <span style="flex:1;color:#1e40af;font-weight:600;">Reservado por <span id="cart-reserve-countdown">5:00</span></span>
      <span style="color:#3b82f6;font-size:0.7rem;">Finalize antes que expire</span>
    </div>
    <!-- Mensagem de expiração -->
    <div id="cart-reserve-expired-msg" style="display:none;margin-bottom:0.75rem;padding:8px 12px;border-radius:8px;background:#FEF2F2;border:1px solid #FCA5A5;color:#991b1b;font-size:0.75rem;font-weight:600;text-align:center;">
      ⚠️ Sua reserva expirou. Os itens foram removidos do carrinho.
    </div>

    <!-- Aviso de mínimo de acessórios para atacado -->
    {% if settings.atacado_acessorio_enabled %}
    <div id="cart-acessorio-warning" style="display:none;margin-bottom:0.75rem;">
      <div id="cart-acessorio-items"></div>
    </div>
    {% endif %}

    <div class="cart-total">
      <span>Total</span>
      <span id="cart-total-price">R$ 0,00</span>
    </div>
    <a href="/checkout" class="cart-checkout-btn" id="cart-checkout-btn" onclick="clearCartReserve()">
      Finalizar Compra
    </a>
    <p style="font-size:0.75rem;color:var(--muted-foreground);text-align:center;margin-top:0.75rem;">
      🔒 Pagamento 100% seguro
    </p>
  </div>
</div>

{% if settings.atacado_acessorio_enabled %}
<script>
(function(){
  // checkAcessorioRule mantido para compatibilidade mas lógica principal está no renderCart
  window.checkAcessorioRule = function(){};
})();
</script>
{% endif %}
